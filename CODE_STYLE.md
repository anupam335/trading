Code style and standards

- Use `black` for formatting. Run: `black .`
- Use `flake8` for linting.
- Functions and classes must have docstrings (Google style preferred).
- Prefer type hints for public functions.
- Write tests with `pytest` and aim for clear fixtures.
- Commit messages follow Conventional Commits.
